1. login to jarvis account.

2. go to GPU directory.

3. compile the program(this case it should be already compiled): make     (use a makefile to compile)

4. type commend to run grep: ./grep + keyword
	for example: ./grep wiki      (search wiki in file)
